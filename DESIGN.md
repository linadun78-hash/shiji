# 拾集 Design System

## Scene

用户在浏览小红书攻略或教程时，从页面右侧打开一个像随身手账的窄抽屉，快速拾取、浏览和整理，不离开当前内容。

## Visual Direction

- 参考已确认的 Stitch 手账像素方案，使用纸张底色、墨色描边、砖红主操作、苔藓绿成功态、芥末黄提示态。
- 像素感仅用于索引、标签和小型装饰；正文使用系统中文字体，保证长内容可读。
- 按钮使用 1px 描边与 3px 实体偏移阴影，按下时位移 2px，形成明确的物理反馈。
- 抽屉最大宽度 430px，固定头部和底部，正文独立滚动。
- 不依赖远程字体、图标库、Tailwind 或 CDN。

## Interaction Rules

- 素材卡默认折叠，展开后显示 AI 整理；原始摘录继续保持二级折叠。
- 设置、素材、报告处于同一抽屉，不使用额外弹窗。
- 所有按钮具备 hover、focus、active、disabled 和 loading 状态。
- 动效仅表达状态变化，持续 150-250ms，并尊重 reduced-motion。

